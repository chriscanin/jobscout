{
  "state": "representative",
  "provenance": "Response to a notification POST to a Discord incoming webhook: HTTP 204 No Content with an empty body (matches the sibling webhook-response.json used by the notifier tests). DISCORD_WEBHOOK_URL is not configured in this environment, so this fixture is synthesized from the documented Execute-Webhook response shape rather than captured from a live call. See https://discord.com/developers/docs/resources/webhook#execute-webhook."
}
